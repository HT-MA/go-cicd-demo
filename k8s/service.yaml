apiVersion: v1
kind: Service
metadata:
  name: go-cicd-demo
  labels:
    app: go-cicd-demo
spec:
  type: ClusterIP
  selector:
    app: go-cicd-demo
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
