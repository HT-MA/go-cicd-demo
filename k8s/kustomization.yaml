apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: go-cicd-demo

# 镜像 tag 会被 CD Pipeline 自动更新
images:
  - name: wojiaomht/go-cicd-demo
    newTag: latest

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
